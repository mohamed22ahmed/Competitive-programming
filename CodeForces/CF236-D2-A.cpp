#include bitsstdc++.h
#define  Mo_Ha_Med  stdios_basesync_with_stdio(0),cin.tie(NULL),cout.tie(NULL);
using namespace std;
int main()
{
    Mo_Ha_Med

    setchara;
    string s;
    cins;
    for(int i=0;is.size();i++)
        a.insert(s[i]);
    if(a.size()%2==0)
        coutCHAT WITH HER!n;
    else
        coutIGNORE HIM!n;
}